# Configuración

Archivos de configuración e infraestructura.

## Archivos

- `.env.example` - Variables de entorno
- `docker-compose.yml` - Infraestructura local (DB, Redis)
- `structure.md` - Estructura de carpetas del proyecto
