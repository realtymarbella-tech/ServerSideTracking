# Taxonomía de Eventos

Definición estándar de eventos capturados en el sistema.

## Archivos

- `event-taxonomy.json` - Definición de eventos, parámetros y ejemplos
- `event-schema.json` - Schema JSON validación de eventos
- `examples/` - Ejemplos reales de payloads
